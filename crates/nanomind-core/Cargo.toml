[package]
name = "nanomind-core"
version = "0.1.0"
edition = "2021"
description = "Quantization, tensor ops, RoPE, attention kernels"

[dependencies]
half = { version = "2", features = ["bytemuck"] }
bytemuck = { version = "1", features = ["derive"] }
